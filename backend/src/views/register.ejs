<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Timber Trend</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <style>
        .container-fluid {
            max-width: 900px; /* Limit container width */
            margin: 20px auto; /* Center the container */
            border-radius: 15px; /* Enhance border radius */
            overflow: hidden; /* Ensure rounded corners are visible */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Soft shadow for depth */
            animation: slideIn 1s ease-in-out; /* Slide-in animation for the container */
        }

        .left-panel {
            background: linear-gradient(to bottom right, #FFA500, #FF4500);
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            height: 100vh;
            animation: fadeInLeft 1s ease-out; /* Animation for the left panel */
        }

        .right-panel {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            animation: fadeInRight 1s ease-out; /* Animation for the right panel */
        }

        .left-panel h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .left-panel p {
            font-size: 1.2rem;
            text-align: center;
        }

        .error-message {
            color: red;
            font-size: 1rem;
            margin-bottom: 1rem;
            display: none;
            text-align: center;
        }

        .form-field {
            opacity: 0;
            animation: fadeInFields 1s forwards; /* Animation for form fields */
        }

        form button {
            transition: background-color 0.3s ease;
        }

        .loader {
            border: 5px solid #f3f3f3;
            border-top: 5px solid #FF4500;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
            display: none;
            margin: 20px auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes fadeInLeft {
            0% { opacity: 0; transform: translateX(-100%); }
            100% { opacity: 1; transform: translateX(0); }
        }

        @keyframes fadeInRight {
            0% { opacity: 0; transform: translateX(100%); }
            100% { opacity: 1; transform: translateX(0); }
        }

        @keyframes fadeInFields {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }

        @keyframes slideIn {
            0% { transform: translateY(-100%); }
            100% { transform: translateY(0); }
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Left Panel -->
            <div class="col-md-6 left-panel">
                <h1>Simplify Management</h1>
                <p>Manage your e-commerce site effortlessly with our dashboard.</p>
                <!-- <img src="/path-to-image.png" alt="Dashboard illustration" class="img-fluid mt-3"> -->
            </div>
            <!-- Right Panel -->
            <div class="col-md-6 right-panel" style="background-color: #f5f4fe;">
                <h2>Create Account</h2>
                <div class="error-message" id="error-message"></div>
                <!-- Form submission directly to the backend -->
                <form id="register-form" action="/api/v1/admin/auth/register" method="POST" class="w-75" enctype="multipart/form-data">
                    <div class="mb-3 form-field">
                        <input type="text" name="username" class="form-control" placeholder="Username" required>
                    </div>
                    <div class="mb-3 form-field">
                        <input type="email" name="email" class="form-control" placeholder="Email address" required>
                    </div>
                    <div class="mb-3 form-field">
                        <input type="text" name="fullname" class="form-control" placeholder="Full Name" required>
                    </div>
                    <div class="mb-3 form-field">
                        <input type="password" name="password" class="form-control" placeholder="Password" required>
                    </div>
                    <div class="mb-3 form-field">
                        <input type="file" name="avatar" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Sign Up</button>
                </form>
                <div class="loader" id="loader"></div>
                <p class="mt-3">Already have an account? <a href="/api/v1/admin/auth/login" class="text-decoration-none">Login</a></p>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
