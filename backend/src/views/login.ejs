<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <style>
      .container-fluid {
        max-width: 900px;
        margin: 20px auto;
        height: 700px;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        animation: slideIn 1s ease-in-out;
        display: flex;
        align-items: center; /* Center content vertically */
      }

      .left-panel,
      .right-panel {
        height: 100%; /* Ensure panels fill container height */
        display: flex;
        flex-direction: column;
        justify-content: center; /* Center content vertically */
        align-items: center; /* Center content horizontally */
        text-align: center; /* Align text for better appearance */
      }

      .left-panel h1,
      .left-panel p,
      .left-panel img {
        margin: 10px 0; /* Add spacing between elements */
      }

      .right-panel form,
      .right-panel .loader,
      .right-panel p {
        margin: 10px 0; /* Add spacing between elements */
      }

      .left-panel {
        background: linear-gradient(to bottom right, #ffa500, #ff4500);
        color: white;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        /* padding: 2rem; */
        height: 100vh;
        animation: fadeInLeft 1s ease-out; /* Animation for the left panel */
      }

      .right-panel {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 5rem;
        height: 100vh;
        animation: fadeInRight 1s ease-out; /* Animation for the right panel */
      }

      .left-panel h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
      }

      .left-panel p {
        font-size: 1.2rem;
        text-align: center;
      }

      .error-message {
        color: red;
        font-size: 1rem;
        margin-bottom: 1rem;
        display: none;
        text-align: center;
      }

      .form-field {
        opacity: 0;
        animation: fadeInFields 1s forwards; /* Animation for form fields */
      }

      form button {
        transition: background-color 0.3s ease;
      }

      .loader {
        border: 5px solid #f3f3f3;
        border-top: 5px solid #ff4500;
        border-radius: 50%;
        width: 40px;
        height: 40px;
        animation: spin 1s linear infinite;
        display: none;
        margin: 20px auto;
      }

      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }

      @keyframes fadeInLeft {
        0% {
          opacity: 0;
          transform: translateX(-100%);
        }
        100% {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes fadeInRight {
        0% {
          opacity: 0;
          transform: translateX(100%);
        }
        100% {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes fadeInFields {
        0% {
          opacity: 0;
        }
        100% {
          opacity: 1;
        }
      }

      @keyframes slideIn {
        0% {
          transform: translateY(-100%);
        }
        100% {
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <!-- Left Panel -->
        <div class="col-md-6 left-panel">
          <h1>Welcome Back!</h1>
          <p>
            Access your account and start managing your e-commerce store
            efficiently.
          </p>
          <!-- <img
            src="/path-to-image.png"
            alt="Dashboard illustration"
            class="img-fluid mt-3"
          /> -->
        </div>
        <!-- Right Panel -->
        <div class="col-md-6 right-panel" style="background-color: #f5f4fe">
          <h2>Login</h2>
          <div class="error-message" id="error-message"></div>
          <form
            id="login-form"
            action="/api/v1/admin/auth/login"
            method="POST"
            class="w-75"
            enctype="application/x-www-form-urlencoded"
          >
            <div class="mb-3 form-field">
              <input
                type="text"
                name="username"
                class="form-control"
                placeholder="username...."
                required
              />
            </div>
            <div class="mb-3 form-field">
              <input
                type="password"
                name="password"
                class="form-control"
                placeholder="Password"
                required
              />
            </div>
            <button type="submit" class="btn btn-primary w-100">Login</button>
          </form>
          <div class="loader" id="loader"></div>
          <!-- <p class="mt-3">
            Don’t have an account?
            <a href="/register" class="text-decoration-none">Sign Up</a>
          </p> -->
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
